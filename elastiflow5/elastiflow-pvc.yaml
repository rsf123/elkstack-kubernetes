apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: geodb
   namespace: elk
spec:
   accessModes:
      - ReadWriteMany
   storageClassName: nfs-storage
   resources:
      requests:
         storage: 10Gi

